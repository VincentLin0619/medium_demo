<% if resource.errors.any? %>
  <article class="message is-danger" data-turbo-cache="false">
    <div class="message-body">
      <ul >
        <% resource.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </article>
<% end %>
